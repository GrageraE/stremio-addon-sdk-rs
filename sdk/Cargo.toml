[package]
name = "stremio-addon-sdk"
version = "0.1.2"
authors = ["Ivo Georgiev <ivo@strem.io>", "Sleeyax <yourd3veloper@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
shuttle = ["dep:shuttle-runtime"]

[dependencies]
# Stremio-core, as of 14 April, 2025
stremio-core = { git = "https://github.com/stremio/stremio-core", rev = "525a1a94963683dd64079d9bf1dd016d40027513", features = ["env-future-send"] }

# Hyper HTTP Server
hyper = { version = "^1", features = ["full"] }
http-body-util = "0.1"
hyper-util = { version = "0.1", features = ["full"] }
tokio = { version = "^1", features = ["net"] }

# HTTPS Server
flexible-hyper-server-tls = "^0.6.0"

# Other utils
serde_json = "1.0"
semver = { version = "1", features = ["serde"] }
percent-encoding = "1.0.1"  # Need old version to support encoding
url = { version = "2.4", features = ["serde"] }
async-trait = "^0.1.88"

# Serverless (Shuttle.dev)
# now_lambda = "0.1.3"
shuttle-runtime = { version = "^0.53.0", optional = true }
